# Recommended project defaults for Codex Orchestra. Explicit model selections
# in the app or CLI still take precedence.
model = "gpt-5.6-sol"
model_reasoning_effort = "high"

[agents]
enabled = true
default_subagent_model = "gpt-5.6-terra"
default_subagent_reasoning_effort = "high"

[features]
hooks = true
